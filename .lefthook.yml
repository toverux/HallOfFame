post-checkout:
  commands:
    configure-git:
      run: git config blame.ignoreRevsFile .git-blame-ignore-revs

pre-commit:
  parallel: true
  commands:
    tsc:
      glob: "*.{ts,tsx,js,json}"
      run: mise build:css-types && mise check:tsc
    oxlint:
      glob: "*.{ts,tsx,js,jsx,mjs,cjs}"
      run: mise exec bun -- bun --bun oxlint --deny-warnings --no-error-on-unmatched-pattern {staged_files}
    oxfmt:
      glob: "*.{ts,tsx,js,jsx,mjs,cjs,json,jsonc,md}"
      run: mise exec bun -- bun --bun oxfmt --check --no-error-on-unmatched-pattern {staged_files}

# Tests run on push, not commit, to keep commits fast.
pre-push:
  parallel: true
  commands:
    test:ui:
      glob: "*.{ts,tsx}"
      run: mise test:ui
    test:cs:
      glob: "*.cs"
      run: mise test:cs
