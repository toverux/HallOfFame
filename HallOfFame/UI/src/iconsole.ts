/**
 * "Intended Console".
 * This is a symbol to use in place of `console` to mark that we are intentionally using `console`,
 * and to avoid being flagged by the linter.
 */
export const iconsole = globalThis.console;
